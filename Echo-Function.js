function echoFunction(inputString) {
  console.log(inputString.length); // Prints the length of the input string
  console.log(inputString); // Prints the string itself
}

// Example usage:
echoFunction("Hello, JavaScript!");
echoFunction("strings are easy");
